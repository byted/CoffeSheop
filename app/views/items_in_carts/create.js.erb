// this script gets executed when a product is added.
productTile = $('#product-<%= @current_item.product_id %>');
productTile.addClass('selected');
productTile.find('.quantity').html("<%= @current_item.quantity %>"); 
$('#total_price').html("Σ <%= number_to_currency(@cart.total_price) %>");
