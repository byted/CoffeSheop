<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= yield :meta %>

    <title><%= content_for?(:title) ? yield(:title) : "CoffeeShop" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

  <%= stylesheet_link_tag "application", :media => "all" %>   
  </head>

  <body class="metrouicss">
    <div class="page <%= controller.controller_name %>">
        <div class="page-header bg-color-red">
            <div class="page-header-content">
                <h2><%= yield :head %></h2>
            </div>
        </div>
        <div class="page-region">
            <div class="page-region-content">
        	   <%= yield %>
            </div>
        </div>
        <div class="charms bg-color-red">
            <%= yield :charm %>
        </div>
    </div>

    <% if notice %>
        <div class="info-bar">
            <h2><%= notice %></h2>
        </div>
    <% elsif alert %>
        <div class="error-bar">
            <h2><%= alert %></h2>
        </div>
    <% end %>
    

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <div id="metrouicss_comment">Styled with <a href="http://metroui.org.ua">Metro UI CSS</a></div>
  </body>
</html>
